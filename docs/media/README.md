# Media

- `hero.svg` — animated hero used by the README and the landing page (no recording needed).
- Add real captures here when you have them and swap the README/landing `<img>` paths:
  - `demo.gif` — 10–15 s screen capture: hover the top edge → click a button → drawer slides down → resize. ~1280×720, keep under ~8 MB.
  - `shot-bar.png`, `shot-drawer.png`, `shot-settings.png` — clean PNG screenshots (light theme looks best).
